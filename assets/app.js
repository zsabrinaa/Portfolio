$(document).ready(function () {
    $('.sidenav').sidenav();
});
$('.parallax').parallax();
 
var typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    typeSpeed: 60,
    loop: true,
    backSpeed: 40,
    showCursor: false,
  });